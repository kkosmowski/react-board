import { ProfileRouteParams } from './profile-route-params.interface';
import { ThreadRouteParams } from './thread-route-params.interface';

export interface RouteParams extends ProfileRouteParams, ThreadRouteParams {
}
